# 使用淘宝镜像作为 npm 源。
# 原因：@anthropic-ai/claude-agent-sdk 的平台原生二进制包（如
# claude-agent-sdk-win32-x64，解压后约 265MB）在公司私有源
# (10.1.7.49:8081 / nexus.jereh.cn) 上下载极慢/下不动，
# 换淘宝镜像可正常拉取。见 CLAUDE.md「版本锁定」一节。
registry=https://registry.npmmirror.com
